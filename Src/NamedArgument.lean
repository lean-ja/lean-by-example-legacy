/-
# 名前付き引数
Lean では，関数に引数を渡すときに順序ではなく名前で引数を指定することができます．
-/

-- `foldl` には `init` という名前の引数がある
#check List.foldl

-- 本来は最初の引数は関数だが，
#eval [1, 2, 3, 4].foldl (· + ·) 0

-- 名前付き引数を使うと，引数の順序を変えることができる
#eval [1, 2, 3, 4].foldl (init := 0) (· + ·)
